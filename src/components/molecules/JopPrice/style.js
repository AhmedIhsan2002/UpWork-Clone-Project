import styled from "styled-components";

export const StyledJopPrice = styled.article`
  .text span:nth-child(1) {
    color: var(--dark-color);
    font-size: 15px;
    font-weight: 500;
    margin-bottom: 3px;
  }

  .text span:nth-child(2) {
    font-size: 13px;
    color: var(--gray-color);
    max-width: 200px;
  }
`;
