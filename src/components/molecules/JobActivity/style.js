import styled from "styled-components";

export const StyledJobActivity = styled.article`
  h3 {
    margin-bottom: 30px;
  }
  .activity p {
    color: var(--gray-color);
  }

  .rates {
    margin-bottom: 70px;
  }

  .rates div {
    margin-bottom: 10px;
  }

  .rates + div h3 {
    font-size: 18px;
    margin: 0;
  }
`;
