import { StyledSection } from "@/style/common";
import styled from "styled-components";

export const StyledJobsList = styled(StyledSection)`
  h3 {
    font-size: 20px;
    font-weight: 500;
    padding: 25px 30px 5px;
  }
`;
