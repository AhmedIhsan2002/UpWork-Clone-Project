import { StyledPage } from "@/style/common";
import styled from "styled-components";

export const StyledProfilePage = styled(StyledPage)`
  margin: 40px auto;

  .content {
    border-top: 1px solid var(--gray-300-color);
  }
`;
